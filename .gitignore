node_modules/
package-lock.json
*.log
.DS_Store
.env

# Contains target MAC / host — keep out of normal commits.
# Use `git add -f ecosystem.config.js` to force-commit when needed.
ecosystem.config.js
